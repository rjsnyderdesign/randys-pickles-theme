<div id="page" class="row">
          <div class="span12 details">
            <h1 class="p0">{{ page.title }}</h1>
            <p class="p25">Check back often to see when Randy's Pickles is mentioned in the news!</p>
            {{ page.content }}
          </div>
        </div>