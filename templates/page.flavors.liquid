<div id="page" class="row">
  
  <div class="details span12">
    <h1 class="p25">{{ page.title }}</h1>
    {{ page.content }}
	</div>
	<div class="products">
	  {% for product in collections.pickles.products %}
	  {% include 'flavor-loop' %}
	  {% endfor %}
	</div>
  
  
</div>